<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis1.helloWorld.EmployeeMapper">
    <select id="selectEmp" resultType="mybatis1.helloWorld.Employee">
        select * from tbl_employee where id = #{id}
    </select>

    <select id="getEmpById" resultType="mybatis1.helloWorld.Employee">
        select * from tbl_employee where id = #{id}
    </select>
</mapper>